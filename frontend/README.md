# VidPilot AI — Frontend

React + TypeScript + Vite + Tailwind CSS. See the [project README](../README.md) for architecture, setup, and Phase 1 scope.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
npm run lint
```
